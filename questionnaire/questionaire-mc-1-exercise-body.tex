% ------------------------------------------------------------------------
% file `questionaire-mc-1-exercise-body.tex'
%
%     exercise of type `mc' with id `1'
%
% generated by the `multiplechoice' environment of the
%   `xsim' package v0.11 (2018/02/12)
% from source `questionaire' on 2019/07/18 on line 60
% ------------------------------------------------------------------------
In Rust, \texttt{vec!} is used to create a vector. All variables in Rust are immutable by default. If this program compiles and runs, what does it print?
\begin{lstlisting}
let v = vec![2,3];
v.push(3);
print!("{:?}", v);
\end{lstlisting}

\choice {[2,3,2]}
\choice {[2,2,2,3]}
\choice The program does not compile.
\choice The program compiles, but crashes when it tries to push 3 to v.

